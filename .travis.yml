sudo: false
language: node_js
node_js:
  - "10.15.1"
branches:
  only:
    - master
install:
  - npm install
  - npm install -g coveralls
cache:
  directories:
    - node_modulesnp
script:
  - npm run test
after_success:
  - npm run coveralls